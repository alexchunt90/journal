{
  "name": "journal",
  "version": "1.0.0",
  "private": true,
  "description": "A local, single-user app. No dependencies, no build step.",
  "license": "UNLICENSED",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "lint": "node scripts/lint.js"
  },
  "engines": {
    "node": ">=18"
  }
}
